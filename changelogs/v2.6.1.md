# v2.6.1

## Fixes

- Deliver visible model text before a proactive Agent call executes a tool that sends media directly, preserving the expected text-then-media order.
- Keep early-delivered text and optional post-tool text in proactive conversation history instead of replacing them with a generic direct-tool placeholder.
- Prevent the final Agent response from sending the same text a second time after early delivery.