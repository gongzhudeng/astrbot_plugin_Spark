# v2.7.5

## Fixed

- The proactive status panel now labels the LLM-judge countdown correctly in "大模型判断" mode: it shows "大模型判断" before judging, "大模型判断重试" while retrying a failed judge call, and only shows "沉寂问候" once the one-shot delivery task exists.