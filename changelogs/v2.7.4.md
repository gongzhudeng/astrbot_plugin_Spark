# v2.7.4

## Fixed

- Treat already-delivered visible texts as success when the Agent pipeline times out, so daily greetings are no longer duplicated with a second message and selfie.
- Advance the daily greeting retry timestamp by the real send duration so the technical-retry backoff is honored instead of firing immediately.