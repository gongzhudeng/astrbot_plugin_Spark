# 灵犀 · 主动对话 更新日志

## v2.6.0

### 新增

- 沉寂判断模型与普通“是/否”智能判断模型独立配置；上下文轮数、人格和实时上下文仍保持共用
- 沉寂问候支持对话热度、固定时间和大模型判断三种互斥模式；模型模式使用严格非负整数分钟协议，并持久化判断周期和一次性候选任务
- 对话增强支持对话热度与固定时间随机浮动两种模式，沉寂问候和对话增强分别提供独立的智能判断开关
- 每日问候的固定时间与日程活动字段按模式条件显示

### 变更

- 用户消息、普通 AI 回复或主动消息会立即使等待中的沉寂候选失效；未完成判断与候选任务可在重启后安全恢复
- 移除废弃的命令式提醒配置、命令和持久化字段；保留每日问候总开关
- 旧热度端点、固定时间浮动和增强等待字段继续作为兼容回退，新字段优先

## v2.5.4

### 修复

- 兼容 AstrBot 4.27.3 移除 `build_main_agent_config` 的核心接口变更，恢复主动对话与 AI 提醒的 Agent Pipeline、知识库、记忆 hook 和框架上下文裁剪能力
- 新增主 Agent 配置适配层；旧版 AstrBot 仍优先复用框架原生构建函数，当前版本则按核心 `MainAgentBuildConfig` 完整映射配置

## v2.5.3

### 新增

- 对话热度改为“短期动量 + 长期余温”的双尺度模型，两者合成为沉寂问候和对话增强共用的热度信号
- 对话增强新增独立的热场/冷场等待端点，默认每轮进入智能判断，由判断模型决定是否真正开口
- 成功送达的自然主动消息新增有界脱敏证据，按来源、送达时间和首次后续回复提供给 Emotion 幂等结算；手动 `/立即主动` 不纳入
- 每日问候新增持久化执行状态与技术失败有限重试，支持可选稳定 `task_id`，旧 session tag 继续兼容
- 智能判断新增只读 `{emotion_state}` 上下文；旧自定义模板缺少该占位符时自动追加，Emotion 不可用时安全降级为空
- 每日问候新增逐条 `ignore_judge` 开关与按问候记录的成功送达间隔；新条目默认执行判断，旧条目迁移后保持原有跳过判断行为

### 修复

- Agent 工具已直发语音或其他媒体时按成功送达处理，不再重复发送文本或触发技术重试；语音原文会随主动占位消息写入标准对话历史。

### 变更

- 沉寂问候和对话增强的等待时间改用非线性几何插值，配置端点保持明确且中等热度响应更自然
- 用户尚未回应上一条主动消息时，不再继续发送沉寂问候或对话增强
- 默认沉寂问候冷场上限调整为 200 分钟，对话增强默认开启且判断概率为 100%
- 主动请求的旧直连模型降级路径在框架支持时也执行请求钩子，并记录不含 prompt 内容的 marker 诊断
- 每日问候仅对模型、网络或平台发送失败重试；聊天冷却、免打扰和忙碌均作为正常终态跳过，错过首次窗口不跨时段补发
- 活动问候对每个关键词匹配活动生成候选，并按同一问候上次成功送达时间限制触发间隔；判断拒绝、策略跳过和技术失败均不刷新间隔

## v2.4.0

### 新增

- 智能判断支持不限数量、可排序的独立模型回退列表
- 内容生成支持插件独立回退列表，并可切换为跟随当前会话的 AstrBot 回退对话模型列表
- Agent Pipeline 与旧版直连生成路径共享相同的候选顺序，并跳过重复、已删除或不可用的模型

## v1.6.8

### 修复

- **主动历史占位符语义修复**：主动对话存入历史时，user 侧从无意义的 `[主动对话]` 改为语义清晰的描述，让模型能正确认领"那条 assistant 消息是我自发发出的"，而不是将其误认为对某条 Mando 消息的回复
- **livingmemory 对话污染修复**：在触发 `OnLLMRequestEvent` 钩子前，临时将 `cron_event.message_str` 替换为干净占位符，使 livingmemory 写入 conversation store 的是占位符而非完整主动触发 prompt；钩子完成后立即恢复生成 prompt，不影响记忆检索效果
- **检索过滤同步**：`_is_natural_retrieval_line` 新增对 `[灵犀主动` 前缀的拦截，防止新占位符混入检索 query

## v1.6.7

### 修复

- **主动回复上下文控制修复**：主动回复现在会显式读取 `gen_history_rounds` 对应的最近真实对话上下文，同时保留 v1.4.0 的钩子能力（记忆召回、知识库注入等）
- **主动回复事实优先级修复**：主动生成会自动附加当前时间、当前活动、节律、最近真实对话等实时事实，并明确长期记忆/知识库不能覆盖当前事实，降低“山姆超市”等旧知识被误当作今天话题的概率
- **主动回复检索查询修复**：记忆召回和知识库检索优先使用最近真实聊天内容，不再直接用“主动回复请求”作为主要查询语义，减少检索跑偏
- **主动回复轻量事实模式**：完整实时事实附加改为可配置模式，默认仅强调“AI 主动开口，不是 Mando 新消息”，避免重复注入活动、节律和最近消息导致主动回复像普通被动应答
- **主动检索查询清洗**：主动检索 query 会过滤命令、系统残留、主动事实块，并限制长度，避免 livingmemory / knowledge_base 因过长或非自然聊天文本跑偏
- **对话增强冷却修复**：增强任务醒来时，如果等待期间已有新消息，或距最近聊天不足冷却时间，会直接跳过，避免刚聊完仍追发
- **配置文案更新**：`生成步骤上下文轮数` 已从"已废弃"改回可用状态，说明该配置会控制主动回复生成时参考的最近真实对话轮数

### 新增

- **分钟级热度窗口**：新增 `heat_window_minutes`，旧 `heat_window_hours` 自动兼容迁移
- **热度严苛度配置**：新增 `heat_messages_for_full_score`，可提高达到满热所需消息数，让热度上升更慢
- **对话增强执行冷却**：新增 `enhancement_cooldown_minutes`，控制增强任务醒来后距最近聊天不足多久时跳过

## v1.6.5

### 新增

- **待触发任务显示对话增强**：`/主动状态` 的"待触发任务"栏新增对话增强行，显示预计触发的剩余分钟数；任务完成或取消后自动清除

## v1.6.4

### 修复

- **真实消息检测逻辑重写**：v1.6.3 的 `raw_message` 方案在 `chat_merger` 等插件处理后 `message_obj.raw_message` 已不是原始 Event，导致真实聊天消息也被误判为命令，`last_user_reply_ts` 和热度时间戳完全停止更新。改为通过 `activated_handlers` 检查：若当前事件激活了 Spark 自身的 `_cmd_*` handler，则判定为命令；否则有文本内容即为真实消息。此方案不依赖任何文本前缀或平台内部字段，对所有消息来源均可靠

## v1.6.3

### 修复

- **斜杠命令不过滤问题（初版，已被 v1.6.4 修正）**：从 `raw_message` 读取原始文本检测 `/` 前缀，但在部分消息处理链中失效

## v1.6.2

### 修复

- **斜杠命令不应重置沉寂倒计时**：`/主动状态` 等命令不再更新 `last_user_reply_ts`、`msg_timestamps`（热度）和 `next_idle_ts`

## v1.6.1

### 新增

- **`/主动状态` 待触发任务栏**：新增"待触发任务"区块，显示沉寂问候剩余倒计时 + 今日每日问候各时段的剩余时间（已触发的不显示）

### 变更

- 移除 `/conversa` 命令注册（英文遗留入口）；`/灵犀` 保持不变，`conversa_subscribe` Agent 工具不受影响

## v1.6.0

### 新增

- **每日问候多模板随机化**：`daily_greetings` 的 `prompt` 字段现在支持字符串列表，每次触发时随机选一条，与沉寂问候/对话增强行为一致
- **内置多条早晨/午间问候模板**：默认配置包含多个早晨问候变体（打招呼、自拍分享、随口闲聊），午间也扩充为两条，增加问候多样性

### 变更

- `_conf_schema.json` 中 `greeting_slot.prompt` 类型由 `text` 改为 `list`，已有单条字符串配置向后兼容

## v1.5.0

### 新增

- **对话热度系统**：独立配置节 `heat_settings`，根据最近消息密度动态调整沉寂问候的触发间隔
  - 热度高（近期密集聊天）→ 冷场后更快主动联系（最短 `hot_delay_minutes`，默认 30 分钟）
  - 热度低（一直偶尔聊）→ 触发间隔保持较长（最长 `cold_delay_minutes`，默认 1200 分钟）
  - 热度使用指数衰减计算，越新的消息权重越高，超出观察窗口自动淡出，平滑过渡
  - 独立开关 `enable_heat`，关闭时完全等价于原来的固定 `idle_after_minutes` 行为
- **`{heat_level}` 占位符**：所有提示词模板（含判断模板）新增 `{heat_level}` 占位符，值为 `冷 / 温 / 热`，可在 judge 模板中让 AI 参考热度决策
- **`/灵犀 debug` 热度信息**：调试输出新增当前热度值、热度等级和基于热度算出的下次触发延迟

## v1.4.1

### 修复

- **上下文污染修复**：主动对话结束后不再把完整触发 prompt 存为 `role: user` 消息。现在 user 侧改为存中性标记 `[主动对话]`，AI 后续回看历史时不会看到导演笔记

## v1.4.0

### 修复

- **钩子注入时序修复**：`OnLLMRequestEvent` 钩子现在在 LLM 调用之前触发（原先在 `runner.step_until_done()` 之后，注入完全无效）。修复后记忆召回（livingmemory）、知识库检索、节律注入均对主动对话生效
- **对话历史加载方式修复**：主动对话不再手动截断历史，改为与正常对话一致，通过 `build_main_agent` 从 `req.conversation` 自动加载完整历史，避免上下文断档

### 优化

- **移除提示词模板中的 `{time_period_prompt}` 占位符**：节律内容现在由 `astrbot_plugin_time_period_prompt` 插件通过钩子自动注入，无需手动配置占位符。已有配置中的该占位符建议手动删除，避免重复注入

### 注意

- 升级后如使用了 `{time_period_prompt}` 占位符，请在 Spark 提示词模板中手动删除，节律内容由插件钩子统一注入
- 建议在 livingmemory 设置中确认"启用跨轮次上下文扩展检索"已开启，以使主动对话也能触发记忆检索

## v1.3.0

### 优化

- **主动对话缓存命中率提升**：主动对话现在通过 `OnLLMRequestEvent` 钩子触发 busy_schedule 插件的注入，使主动对话的 system_prompt 与正常对话保持一致，共享 KV Cache，提高缓存命中率，降低 API 调用成本
- **清理提示词模板**：移除提示词模板中的 busy_schedule 插件占位符（`{today_schedule}`, `{outfit}`, `{current_activity}`, `{next_activity}`, `{custom_prompt}`），改为由 busy_schedule 插件通过钩子自动注入
- **人设注入方式统一**：主动对话现在通过 `_ensure_persona_and_skills` 方法注入人设，与正常对话保持一致，进一步提升缓存命中率

### 新增

- **灵犀AI忙碌时段管理插件集成说明**：在 README 中添加了对 astrbot_plugin_busy_schedule 插件的集成说明，安装即生效，无需配置

## v1.2.6

### 修复

- 修复沉寂问候在智能判断返回"否"后不标记已触发标记，导致调度器每 30 秒重复调用 LLM 判断直到碰巧返回"是"的问题。现在判断结果为"否"时也会标记该分钟已触发，避免无意义的重复调用。

## v1.2.1

### 修复

- 智能判断 LLM 调用失败时自动重试最多 3 次（指数退避），仅对临时性错误重试（502/503/504/超时/空响应）
- 判断失败后不再静默跳过，日志会记录重试过程和最终失败原因

## v1.0.0

基于 [astrbot_plugin_Conversa v3.0.0](https://github.com/Luna-channel/astrbot_plugin_Conversa) 二次开发，灵感参考：Luna-channel。

### 新增

- **两步式主动对话流程**：第一步 LLM 智能判断是否适合开口，第二步生成回复内容；两步可独立配置供应商和人格
- **无限每日定时问候**：支持添加任意数量的问候时段，每个时段独立控制 `ignore_dnd`（是否跳过免打扰和忙碌）
- **忙碌时段免打扰**：与 `astrbot_plugin_busy_schedule` 深度集成，忙碌期间自动静默
- **中文指令**：`/灵犀`、`/立即主动`、`/主动状态`、`/主动帮助` 及子命令中文别名
- **人格下拉选择**：判断用人格和生成用人格支持 `_special: select_persona` 下拉选择

### 改造

- 从 Conversa v3.0.0 fork，插件名重命名为 `astrbot_plugin_Spark`
- 旧的句号拦截逻辑替换为 LLM 判断步骤
- 固定 3 时段每日问候迁移为列表格式，支持无限添加
- 旧版配置自动迁移（`daily1/2/3` → 列表、`special.provider` → `advanced.fixed_provider` 等）
- 所有日志前缀 `[Conversa]` → `[Spark]`
- 命令系统保留 `/conversa` 兼容，新增 `/灵犀` 中文主命令
- 主动回复走 AstrBot 官方 Agent Pipeline（继承自 Conversa v3.0.0）

### 保留的上游功能

- 沉寂问候（基准时间 + 随机波动）
- 私聊对话增强（概率追回复）
- Agent 订阅模式（manual / auto / agent）
- 免打扰时段（全局 + 用户专属）
- 自动退订 & 重新激活
- 提醒事项迁移到 AstrBot 原生 cron 系统