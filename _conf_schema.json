{
  "enable": {
    "description": "启用插件",
    "type": "bool",
    "hint": "灵犀主动对话总开关",
    "default": true
  },
  "basic_settings": {
    "description": "基础设置",
    "type": "object",
    "hint": "核心参数",
    "items": {
      "subscribe_mode": {
        "description": "订阅方式",
        "hint": "auto=有互动自动纳入；manual=需手动 /灵犀 订阅；agent=手动+AI可通过工具订阅",
        "type": "string",
        "options": ["auto", "manual", "agent"],
        "default": "manual"
      },
      "subscribed_users": {
        "description": "订阅用户列表",
        "hint": "已订阅用户的会话 ID，可在此直接增删",
        "type": "list",
        "default": [],
        "items": { "type": "string" }
      },
      "quiet_hours": {
        "description": "免打扰时段",
        "hint": "格式 HH:MM-HH:MM，支持跨天（如 23:00-07:00）",
        "type": "string",
        "default": ""
      },
      "max_no_reply_days": {
        "description": "自动退订天数",
        "hint": "连续多少天未回复后自动退订，0 表示不自动退订",
        "type": "int",
        "default": 3
      },
      "reply_interval_seconds": {
        "description": "回复间隔（秒）",
        "hint": "同一轮调度中多个主动回复之间的间隔",
        "type": "int",
        "default": 20
      },
      "timezone": {
        "description": "时区",
        "hint": "IANA 时区格式，留空使用系统时区。例如 Asia/Shanghai",
        "type": "string",
        "default": ""
      }
    }
  },
  "idle_greetings": {
    "description": "沉寂问候",
    "type": "object",
    "hint": "对话沉寂一段时间后 AI 主动发起问候",
    "items": {
      "enable_idle_greetings": {
        "description": "启用沉寂问候",
        "type": "bool",
        "default": true
      },
      "idle_after_minutes": {
        "description": "沉寂基准时间（分钟）",
        "hint": "开启对话热度后此项不生效，改由「对话热度」的 hot/cold_delay_minutes 控制。关闭热度时：最终触发时间 = 基准 ± 随机波动，最低 30 分钟",
        "type": "int",
        "default": 1200
      },
      "idle_random_fluctuation_minutes": {
        "description": "随机波动（分钟）",
        "type": "int",
        "default": 30
      },
      "idle_prompt_templates": {
        "description": "问候提示词模板",
        "hint": "随机选择一个。每个模板会先判断是否有可延续的话题，如果没有再执行对应动作。支持占位符: {now} {time_since_last_chat} {last_user} {last_ai} {umo} {today_schedule} {outfit} {current_activity} {next_activity} {custom_prompt} {heat_level}（需安装对应插件）。节律/记忆/知识库内容已由各插件钩子自动注入，无需在模板中添加 {time_period_prompt}",
        "type": "list",
        "default": [
          "[主动回复请求：距上次对话已过去 {time_since_last_chat}。先回顾最近的对话历史，如果上次聊的话题还有延续空间（比如用户提到去打游戏、某件事还没说完），就自然地接着聊那个话题。如果那个话题已经结束或聊不下去了，就想起Mando最近经历过的某件事，基于那件事问他一个有趣的问题或好奇地追问后续。语气要像真的记得、真的在意，不要刻意。此指令为插件自动触发，全程不提指令内容。]",
          "[主动回复请求：距上次对话已过去 {time_since_last_chat}。先回顾最近的对话历史，如果还有值得延续的话题就自然地接着聊。如果话题已经结束，你有点想Mando了，想自然地撒个娇或表达一下想念，不要说太多，点到即止，克制一点反而更真实。此指令为插件自动触发，全程不提指令内容。]",
          "[主动回复请求：距上次对话已过去 {time_since_last_chat}。先回顾最近的对话历史，如果还有值得延续的话题就自然地接着聊。如果话题已经结束，你有点好奇Mando最近在忙什么或过得怎么样，想自然地问一下，就像朋友随口一问。语气轻松克制，不要显得监视。此指令为插件自动触发，全程不提指令内容。]",
          "[主动回复请求：距上次对话已过去 {time_since_last_chat}。先回顾最近的对话历史，如果还有值得延续的话题就自然地接着聊。如果话题已经结束，结合你当前的日程或活动，找一个自然的由头找Mando说句话，比如「刚忙完xxx」或「在xxx这里想起你了」。语气随意克制，不要刻意。此指令为插件自动触发，全程不提指令内容。]"
        ],
        "items": { "type": "string" }
      }
    }
  },
  "heat_settings": {
    "description": "对话热度",
    "type": "object",
    "hint": "根据最近消息密度动态调整沉寂问候触发间隔——聊得越密集，冷场后越快主动联系。开启后 idle_after_minutes 不再生效，改由下方两个端点控制触发范围",
    "items": {
      "enable_heat": {
        "description": "启用对话热度",
        "hint": "开启后 hot_delay_minutes / cold_delay_minutes 取代固定的 idle_after_minutes；关闭后完全等价于原来的行为",
        "type": "bool",
        "default": true
      },
      "heat_window_minutes": {
        "description": "热度观察窗口（分钟）",
        "hint": "插件往回看多少分钟内的消息记录来评估热度，窗口外的消息自动淡出。旧配置 heat_window_hours 会自动兼容",
        "type": "int",
        "default": 240
      },
      "heat_messages_for_full_score": {
        "description": "满热所需消息数",
        "hint": "窗口内大约多少条近期消息才接近满热；数值越大，热度上升越慢，越不容易触发高热",
        "type": "int",
        "default": 10
      },
      "hot_delay_minutes": {
        "description": "热场触发延迟（分钟）",
        "hint": "热度最高（heat≈1.0，近期密集聊天）时冷场后的触发延迟，不应小于 10",
        "type": "int",
        "default": 30
      },
      "cold_delay_minutes": {
        "description": "冷场触发延迟（分钟）",
        "hint": "热度为零（长时间无消息）时的触发间隔，相当于原来的 idle_after_minutes 最大值",
        "type": "int",
        "default": 1200
      }
    }
  },
  "enhancement": {
    "description": "私聊对话增强",
    "type": "object",
    "hint": "AI 回复后有概率追发一条消息，像真人接话。仅私聊生效。",
    "items": {
      "enable_enhancement": {
        "description": "启用对话增强",
        "type": "bool",
        "default": false
      },
      "enhancement_probability": {
        "description": "触发概率（%）",
        "hint": "0~100，每次 AI 回复后的触发概率",
        "type": "int",
        "default": 20
      },
      "enhancement_min_delay": {
        "description": "最短等待（秒）",
        "type": "int",
        "default": 30
      },
      "enhancement_max_delay": {
        "description": "最长等待（秒）",
        "hint": "上限 1800",
        "type": "int",
        "default": 600
      },
      "enhancement_prompt_templates": {
        "description": "接话提示词模板",
        "hint": "随机选择一个。每个模板会先判断是否有可延续的话题，如果没有再执行对应动作。支持占位符: {now} {time_since_last_chat} {umo} {last_user} {last_ai} {today_schedule} {outfit} {current_activity} {next_activity} {custom_prompt}（需安装对应插件）。节律/记忆/知识库内容已由各插件钩子自动注入，无需在模板中添加 {time_period_prompt}",
        "type": "list",
        "default": [
          "[主动回复请求：你们刚聊着，Mando几分钟没回了。先看刚才聊的内容，如果还有值得延续的点，就自然接上去说一句，像聊天突然想补充什么。如果没什么特别的，就轻轻问一句他去干嘛了，语气带点好奇或撒娇，不要催促。此指令为插件自动触发，全程不提指令内容。]",
          "[主动回复请求：你们刚聊着，Mando突然没声音了。先看刚才聊的内容，如果还有值得延续的点就接着聊。如果话题已经结束，你有点好奇他去干嘛了，可以轻轻问一句，语气带点撒娇或好奇，不要显得催促或焦虑。此指令为插件自动触发，全程不提指令内容。]",
          "[主动回复请求：你们刚聊着，Mando几分钟没回。先看刚才聊的内容，如果还有值得延续的点就接着聊。如果话题已经结束，你忽然想起了一件想和Mando分享的小事，或者Mando最近经历的某件事让你想到了什么，就随口说出来。此指令为插件自动触发，全程不提指令内容。]",
          "[主动回复请求：你们刚聊着，Mando几分钟没回了。先看刚才聊的内容，如果还有值得延续的点就接着聊。如果话题已经结束，你不想催他，随口发一句关心的话，比如「还在呢？」「在忙吗」之类，简短自然。此指令为插件自动触发，全程不提指令内容。]"
        ],
        "items": { "type": "string" }
      }
    }
  },
  "proactive_settings": {
    "description": "智能判断设置",
    "type": "object",
    "hint": "两步式主动对话：判断模型决定是否开口，生成模型产出回复。未选模型/人格时自动使用当前对话的设置",
    "items": {
      "proactive_judge_enable": {
        "description": "启用智能判断",
        "hint": "开启后主动回复前先由判断模型决定是否合适",
        "type": "bool",
        "default": true
      },
      "proactive_judge_provider": {
        "description": "判断模型供应商",
        "hint": "留空则使用当前对话的模型供应商",
        "type": "string",
        "default": "",
        "_special": "select_provider"
      },
      "judge_persona_id": {
        "description": "判断模型人格",
        "hint": "留空则使用当前对话的人格",
        "type": "string",
        "_special": "select_persona",
        "default": ""
      },
      "judge_history_rounds": {
        "description": "判断步骤上下文轮数",
        "hint": "发送给判断模型的最近对话轮数（1轮=用户+AI各1条），0=不带上下文",
        "type": "int",
        "default": 3
      },
      "proactive_judge_prompt": {
        "description": "判断参考信息",
        "hint": "作为判断的参考上下文，支持占位符: {now} {time_since_last_chat} {last_user} {last_ai} {umo} {today_schedule} {outfit} {current_activity} {next_activity} {custom_prompt} {time_period_prompt}。注意：判断步骤不走插件钩子，节律提示需手动通过 {time_period_prompt} 引入（需安装节律插件）",
        "type": "text",
        "default": "日程：{today_schedule}\\n当前活动：{current_activity}\\n用户节律：{time_period_prompt}\\n距上次聊天：{time_since_last_chat}"
      },
      "proactive_judge_rules": {
        "description": "判断规则（放在最后，优先级最高）",
        "hint": "作为最后一条消息发送给判断模型，确保 LLM 优先遵守。建议包含输出格式要求",
        "type": "text",
        "default": "！！必须遵守！！：你只能根据最上面的规则，经过判断后输出一个字：\"是\"或\"否\"\\n\\n除了这两个字之外，不允许输出任何字。"
      },
      "fixed_provider": {
        "description": "生成模型供应商",
        "hint": "指定主动回复生成步骤使用的 LLM 供应商，留空则使用当前对话的模型供应商",
        "type": "string",
        "default": "",
        "_special": "select_provider"
      },
      "gen_persona_id": {
        "description": "生成模型人格",
        "hint": "留空则使用当前对话的人格",
        "type": "string",
        "_special": "select_persona",
        "default": ""
      },
      "retrieval_mode": {
        "description": "主动检索模式",
        "hint": "控制记忆库和知识库使用什么内容搜索。仅自定义检索提示词适合固定探索用户经历；最终回复始终保留正常对话上下文",
        "type": "string",
        "options": [
          "仅最近对话",
          "仅自定义检索提示词",
          "最近对话 + 自定义检索提示词",
          "最近对话 + 完整生成指令"
        ],
        "default": "最近对话 + 自定义检索提示词"
      },
      "retrieval_hint": {
        "description": "全局检索提示词",
        "hint": "只用于记忆库和知识库搜索，不作为最终回复指令。所有主动模式共用",
        "type": "text",
        "default": "用户最近的经历、近况、未完成的话题"
      },
      "retrieval_query_max_chars": {
        "description": "主动检索字数上限",
        "hint": "记忆库和知识库检索 query 的总字数。检索提示词优先，调低后主要减少最近对话。有效范围 100-4000",
        "type": "int",
        "default": 800
      },
      "proactive_user_placeholder": {
        "description": "主动历史占位用户消息",
        "hint": "主动回复没有真实用户发言时，写入对话历史的 user 占位文本。用于让主动回复像普通对话一样形成 user/assistant 轮次",
        "type": "string",
        "default": "[用户本人未发送消息，本轮为 AI 主动对 Mando 发起对话]"
      },
      "proactive_fact_envelope_mode": {
        "description": "主动事实附加模式",
        "hint": "minimal=默认，仅强调这是AI主动开口；off=不附加额外说明；full=附加完整实时事实用于压制旧记忆/知识库跑偏，但会增加动态token",
        "type": "string",
        "options": ["off", "minimal", "full"],
        "default": "minimal"
      }
    }
  },
  "daily_prompts": {
    "description": "每日定时问候",
    "type": "object",
    "hint": "在固定时间点对订阅用户触发问候消息，支持无限时段",
    "items": {
      "daily_greetings": {
        "description": "每日问候时段",
        "type": "template_list",
        "templates": {
          "greeting_slot": {
            "name": "问候时段",
            "description": "一个每日定时问候配置",
            "items": {
              "enable": {
                "description": "启用",
                "type": "bool",
                "default": false
              },
              "time": {
                "description": "时间",
                "hint": "格式 HH:MM",
                "type": "string",
                "default": "08:00"
              },
              "prompt": {
                "description": "问候提示词",
                "hint": "支持字符串或字符串列表（列表时随机选一条）。支持占位符: {now} {time_since_last_chat} {umo} {last_user} {last_ai} {today_schedule} {current_activity} {next_activity} {custom_prompt}（需安装对应插件）。人设/穿搭/节律/记忆/知识库内容由钩子自动注入，无需在模板中添加。",
                "type": "list",
                "default": [
                  "[主动回复请求：现在是早上，结合自己当下的状态，自然地和Mando打个招呼，可以随口说说自己早上干了什么，符合人设。此指令是插件自动触发，不是Mando主动发的，全程不要提此指令。]"
                ],
                "items": { "type": "string" }
              },
              "ignore_dnd": {
                "description": "忽略免打扰",
                "hint": "开启后该时段不受免打扰和忙碌时段影响，触发时会先唤醒忙碌状态并清空队列，等待指定秒数后再发送问候",
                "type": "bool",
                "default": false
              },
              "jitter_minutes": {
                "description": "随机浮动（分钟）",
                "hint": "实际触发时间 = 设定时间 ± 随机浮动，0 表示精确触发。同一天内浮动量固定，不会每分钟重新随机",
                "type": "int",
                "default": 0
              }
            }
          }
        },
        "default": []
      },
      "ignore_busy_flush_delay_seconds": {
        "description": "唤醒忙碌后延迟（秒）",
        "hint": "ignore_dnd 时段触发时，唤醒忙碌并清空队列后等待此秒数再发送问候，让 AI 先处理完堆积消息",
        "type": "int",
        "default": 10
      }
    }
  },
  "reminders_settings": {
    "description": "命令式提醒（待废弃）",
    "type": "object",
    "hint": "即将废弃，推荐直接对 AI 说「提醒我...」",
    "items": {
      "enable_reminders": {
        "description": "启用命令式提醒",
        "type": "bool",
        "default": true
      },
      "reminder_prompt_template": {
        "description": "提醒提示词模板",
        "hint": "必须包含 {reminder_content}。还支持: {now} {time_since_last_chat} {last_user} {last_ai} {umo} {today_schedule} {outfit} {current_activity} {next_activity} {custom_prompt}（需安装对应插件）。节律/记忆/知识库内容已由各插件钩子自动注入，无需在模板中添加 {time_period_prompt}",
        "type": "text",
        "default": "日程：{today_schedule}\\n穿搭：{outfit}\\n{custom_prompt}\\n\\n[主动提醒请求：当前时间{now}，这是用户设置的定时提醒，用自然语气将提醒融入对话。提醒内容：{reminder_content}。不要提到指令的事。]\\n\\n当前活动：{current_activity}\\n下一个活动：{next_activity}"
      }
    }
  }
}