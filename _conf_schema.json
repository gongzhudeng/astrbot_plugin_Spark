{
  "enable": {
    "description": "启用插件",
    "type": "bool",
    "hint": "灵犀主动对话总开关",
    "default": true
  },
  "basic_settings": {
    "description": "基础设置",
    "type": "object",
    "hint": "核心参数",
    "items": {
      "subscribe_mode": {
        "description": "订阅方式",
        "hint": "auto=有互动自动纳入；manual=需手动 /灵犀 订阅；agent=手动+AI可通过工具订阅",
        "type": "string",
        "options": ["auto", "manual", "agent"],
        "default": "manual"
      },
      "subscribed_users": {
        "description": "订阅用户列表",
        "hint": "已订阅用户的会话 ID，可在此直接增删",
        "type": "list",
        "default": [],
        "items": { "type": "string" }
      },
      "quiet_hours": {
        "description": "免打扰时段",
        "hint": "格式 HH:MM-HH:MM，支持跨天（如 23:00-07:00）",
        "type": "string",
        "default": ""
      },
      "max_no_reply_days": {
        "description": "自动退订天数",
        "hint": "连续多少天未回复后自动退订，0 表示不自动退订",
        "type": "int",
        "default": 3
      },
      "reply_interval_seconds": {
        "description": "回复间隔（秒）",
        "hint": "同一轮调度中多个主动回复之间的间隔",
        "type": "int",
        "default": 20
      },
      "timezone": {
        "description": "时区",
        "hint": "IANA 时区格式，留空使用系统时区。例如 Asia/Shanghai",
        "type": "string",
        "default": ""
      },
      "status_image_theme": {
        "description": "主动状态图片主题",
        "hint": "自动切换在 07:00-18:59 使用白天主题，其余时间使用夜间主题。时间按本插件时区判断。",
        "type": "string",
        "options": ["自动切换", "白天模式", "夜间模式"],
        "default": "自动切换"
      }
    }
  },
  "enhancement": {
    "description": "私聊对话增强",
    "type": "object",
    "hint": "AI 回复后有概率追发一条消息，像真人接话。仅私聊生效。",
    "items": {
      "enable_enhancement": {
        "description": "启用对话增强",
        "hint": "每次普通回复后安排一次候选检查。最终是否开口由模式和“忽略智能判断”决定。",
        "type": "bool",
        "default": true
      },
      "mode": {
        "description": "对话增强模式",
        "hint": "对话热度模式根据聊天密度等待；固定时间模式按固定等待时间加随机浮动等待。",
        "type": "string",
        "options": ["对话热度", "固定时间"],
        "default": "对话热度"
      },
      "ignore_judge": {
        "description": "忽略智能判断",
        "hint": "打开后候选任务到点直接生成追回复；关闭后先使用通用智能判断。",
        "type": "bool",
        "default": false
      },
      "fixed_delay_seconds": {
        "description": "固定等待时间（秒）",
        "hint": "固定时间模式使用。普通 AI 回复完成后，等待此时间再执行候选检查。",
        "type": "int",
        "condition": {"mode": "固定时间"},
        "default": 120
      },
      "fixed_random_fluctuation_seconds": {
        "description": "固定等待随机浮动（秒）",
        "hint": "固定时间模式使用。0 不变化；30 表示前后随机 30 秒；+30 固定延后；-30 固定提前；-30~+30 表示随机区间。",
        "type": "string",
        "condition": {"mode": "固定时间"},
        "default": "30"
      },
      "enhancement_probability": {
        "description": "进入智能判断的概率（%）",
        "hint": "建议保持 100，让智能判断负责是否真正开口",
        "type": "int",
        "default": 100
      },
      "enhancement_hot_delay_seconds": {
        "description": "热场等待（秒）",
        "hint": "热度最高时等待多久进入智能判断",
        "type": "int",
        "condition": {"mode": "对话热度"},
        "default": 45
      },
      "enhancement_cold_delay_seconds": {
        "description": "冷场等待（秒）",
        "hint": "热度为零时等待多久进入智能判断；与热场等待之间使用非线性映射，上限 1800",
        "type": "int",
        "condition": {"mode": "对话热度"},
        "default": 600
      },
      "enhancement_prompt_templates": {
        "description": "接话提示词模板",
        "hint": "使用一条综合接话模板。支持占位符: {now} {time_since_last_chat} {umo} {last_user} {last_ai} {today_schedule} {outfit} {current_activity} {next_activity} {custom_prompt}（需安装对应插件）。节律/记忆/知识库内容已由各插件钩子自动注入，无需在模板中添加 {time_period_prompt}",
        "type": "list",
        "default": [
          "[主动回复请求：你们刚聊着，Mando几分钟没回。先看刚才的内容，如果还有值得延续的点，就像聊天时忽然想到补充内容一样自然接上；如果话题已经结束，则根据当下情境选择一种最自然的方式：随口分享一件小事、轻轻问Mando去做什么了、带一点好奇或撒娇地关心一下，或者简短确认他是不是在忙。不要催促、不要焦虑、不要连续追问，语气简短自然。此指令为插件自动触发，全程不提指令内容。]"
        ],
        "items": { "type": "string" }
      }
    }
  },
  "idle_greetings": {
    "description": "沉寂问候",
    "type": "object",
    "hint": "对话沉寂一段时间后 AI 主动发起问候",
    "items": {
      "enable_idle_greetings": {
        "description": "启用沉寂问候",
        "type": "bool",
        "default": true
      },
      "require_user_reply_before_idle_greeting": {
        "description": "等待用户回复后再沉寂问候",
        "hint": "打开后，上一条主动消息未获用户回复时，不再发起新的沉寂问候或沉寂判断；关闭后仍按所选模式继续安排。",
        "type": "bool",
        "default": true
      },
      "mode": {
        "description": "沉寂问候模式",
        "hint": "三种模式互斥：对话热度根据聊天密度安排；固定时间按基准时间安排；大模型判断在无聊天达到阈值后让模型决定多少分钟后再联系。",
        "type": "string",
        "options": ["对话热度", "固定时间", "大模型判断"],
        "default": "对话热度"
      },
      "ignore_judge": {
        "description": "忽略智能判断",
        "hint": "对话热度/固定时间模式下，打开后到点直接生成主动消息；大模型判断模式仍会使用“判断多长时间”来决定是否创建一次性任务。",
        "type": "bool",
        "default": false
      },
      "idle_judge_provider": {
        "description": "沉寂判断模型",
        "hint": "仅大模型判断模式使用。留空则使用当前会话模型；与“智能判断”使用独立配置。",
        "type": "string",
        "condition": {"mode": "大模型判断"},
        "default": "",
        "_special": "select_provider"
      },
      "idle_judge_fallback_providers": {
        "description": "沉寂判断回退模型列表",
        "hint": "仅大模型判断模式使用。主模型失败或输出不符合整数协议时按此顺序切换；与“智能判断”使用独立配置。",
        "type": "list",
        "condition": {"mode": "大模型判断"},
        "items": { "type": "string" },
        "default": [],
        "_special": "select_providers"
      },
      "idle_after_minutes": {
        "description": "固定沉寂时间（分钟）",
        "hint": "固定时间模式使用。每次用户或 AI 消息后重新计时。",
        "type": "int",
        "condition": {"mode": "固定时间"},
        "default": 1200
      },
      "idle_random_fluctuation_minutes": {
        "description": "固定时间随机浮动（分钟）",
        "hint": "固定时间模式使用。0 不变化；5 表示前后随机 5 分钟；+5 固定延后；-5 固定提前；-5~+5、+15~+25 表示在区间内随机。",
        "type": "string",
        "condition": {"mode": "固定时间"},
        "default": "30"
      },
      "hot_delay_minutes": {
        "description": "热场触发延迟（分钟）",
        "hint": "对话热度最高时，沉寂多久后触发；建议不小于 10。",
        "type": "int",
        "condition": {"mode": "对话热度"},
        "default": 30
      },
      "cold_delay_minutes": {
        "description": "冷场触发延迟（分钟）",
        "hint": "对话热度为零时，沉寂多久后触发；与热场延迟之间使用非线性映射。",
        "type": "int",
        "condition": {"mode": "对话热度"},
        "default": 200
      },
      "judge_after_minutes": {
        "description": "无聊天时间（分钟）",
        "hint": "大模型判断模式使用。用户或 AI 连续这么久没有任何聊天后，才调用一次“判断多长时间”模型。",
        "type": "int",
        "condition": {"mode": "大模型判断"},
        "default": 60
      },
      "judge_min_delay_minutes": {
        "description": "最短总沉寂时长（分钟）",
        "hint": "模型返回的是从最后聊天起算的总沉寂分钟数，正整数不得小于此值；0 始终表示本周期不创建任务。",
        "type": "int",
        "condition": {"mode": "大模型判断"},
        "default": 5
      },
      "judge_max_delay_minutes": {
        "description": "最长总沉寂时长（分钟）",
        "hint": "模型返回的总沉寂分钟数超过此范围将视为协议错误，不创建任务。",
        "type": "int",
        "condition": {"mode": "大模型判断"},
        "default": 1440
      },
      "judge_retry_minutes": {
        "description": "判断技术失败重试（分钟）",
        "hint": "仅模型不可用、空响应、非整数或越界时使用；模型返回 0 不重试。",
        "type": "int",
        "condition": {"mode": "大模型判断"},
        "default": 5
      },
      "idle_judge_prompt": {
        "description": "判断多长时间参考信息",
        "hint": "达到无聊天时间后发送给判断模型。额外支持 {last_chat_at} {idle_elapsed_minutes} {idle_min_total_minutes} {idle_max_total_minutes} {busy_status} {busy_periods}；判断可在忙碌时段执行，但候选不能落入忙碌区间。",
        "type": "text",
        "condition": {"mode": "大模型判断"},
        "default": "当前时间：{now}\\n最后聊天时间：{last_chat_at}\\n已沉寂：{idle_elapsed_minutes} 分钟（{time_since_last_chat}）\\n日程：{today_schedule}\\n当前活动：{current_activity}\\n下一个活动：{next_activity}\\n忙碌状态：{busy_status}\\n忙碌时间线：\\n{busy_periods}\\n用户节律：{time_period_prompt}\\n内心世界：\\n{emotion_state}\\n最近用户消息：{last_user}\\n最近AI回复：{last_ai}\\n\\n请先根据聊天记录、日程与忙碌时间线确定合适的绝对联系时刻，再输出从最后聊天时间起算的总沉寂分钟数。当前忙碌不是返回 0 的理由；目标落入忙碌区间时，应顺延到该区间结束后 5 分钟。不要扣除或叠加无聊天判断阈值。",
        "items": {"type": "string"}
      },
      "idle_judge_rules": {
        "description": "判断多长时间规则",
        "hint": "作为最后一条消息发送给判断模型，优先级最高。",
        "type": "text",
        "condition": {"mode": "大模型判断"},
        "default": "！！必须遵守！！：只能输出一个 ASCII 非负整数，不要输出单位、解释、标点、JSON 或其他文字。0 只表示本周期不创建任务；正整数表示从最后聊天时间起到目标联系时刻的总沉寂分钟数，必须位于 {idle_min_total_minutes}~{idle_max_total_minutes}。如果目标时刻已经到达或过去，仍输出对应的正整数；若合适时刻就是现在，则输出 {idle_elapsed_minutes}。不得把无聊天判断阈值从结果中扣除或再次叠加。候选不得落入 {busy_periods} 中的忙碌区间，落入时顺延到区间结束后 5 分钟。",
        "items": {"type": "string"}
      },
      "cooldown_minutes": {
        "description": "聊天冷却（分钟）",
        "hint": "距用户消息、普通 AI 回复或任意主动消息不足此时间时，将延时问候顺延到冷却结束；0 表示关闭",
        "type": "int",
        "default": 10
      },
      "idle_prompt_templates": {
        "description": "问候提示词模板",
        "hint": "使用一条综合模板：优先延续话题，否则根据近况、想念或当前日程自然开口。支持占位符: {now} {time_since_last_chat} {last_user} {last_ai} {umo} {today_schedule} {outfit} {current_activity} {next_activity} {custom_prompt} {heat_level}（需安装对应插件）。节律/记忆/知识库内容已由各插件钩子自动注入，无需在模板中添加 {time_period_prompt}",
        "type": "list",
        "default": [
          "[主动回复请求：距上次对话已过去 {time_since_last_chat}。先回顾最近的对话历史：如果上次话题还有延续空间或有事情没说完，就像真的记得一样自然接着聊；如果已经聊完，则从以下方向中选择最符合当下情境的一种自然开口：想起Mando最近经历的事并追问后续、表达一点克制的想念或撒娇、轻松问问Mando最近在忙什么或过得怎么样、结合当前日程或活动分享自己的状态。也可以按情境拍一张照片或发一段语音，让表达更自然，但不要每次都强行使用。语气真实、随意、克制，不要显得监视或刻意。此指令为插件自动触发，全程不提指令内容。]"
        ],
        "items": { "type": "string" }
      }
    }
  },
  "heat_settings": {
    "description": "对话热度",
    "type": "object",
    "hint": "根据最近消息密度动态调整沉寂问候触发间隔——聊得越密集，冷场后越快主动联系。开启后 idle_after_minutes 不再生效，改由下方两个端点控制触发范围",
    "items": {
      "enable_heat": {
        "description": "启用对话热度计算",
        "hint": "保留热度计算作为可复用的聊天密度指标。沉寂问候是否使用热度，由“沉寂问候模式”单独决定。",
        "type": "bool",
        "default": true
      },
      "heat_window_minutes": {
        "description": "短期热度窗口（分钟）",
        "hint": "衡量刚才是否密集聊天；旧配置继续直接作为短期窗口使用",
        "type": "int",
        "default": 45
      },
      "heat_long_window_minutes": {
        "description": "长期余温窗口（分钟）",
        "hint": "衡量近半天到一天是否聊过，避免短期窗口结束后热度立即归零；建议 480~1440",
        "type": "int",
        "default": 720
      },
      "heat_short_weight": {
        "description": "短期热度权重",
        "hint": "0~1，剩余权重用于长期余温；0.7 表示短期 70%、长期 30%",
        "type": "float",
        "default": 0.7
      },
      "heat_messages_for_full_score": {
        "description": "满热所需消息数",
        "hint": "窗口内大约多少条近期消息才接近满热；数值越大，热度上升越慢，越不容易触发高热",
        "type": "int",
        "default": 10
      }
    }
  },
  "proactive_settings": {
    "description": "智能判断设置",
    "type": "object",
    "hint": "两步式主动对话：判断模型决定是否开口，生成模型产出回复。未选模型/人格时自动使用当前对话的设置",
    "items": {
      "proactive_judge_enable": {
        "description": "启用智能判断",
        "hint": "开启后主动回复前先由判断模型决定是否合适",
        "type": "bool",
        "default": true
      },
      "proactive_judge_provider": {
        "description": "判断模型供应商",
        "hint": "留空则使用当前对话的模型供应商",
        "type": "string",
        "default": "",
        "_special": "select_provider"
      },
      "proactive_judge_fallback_providers": {
        "description": "判断回退模型列表",
        "hint": "判断主模型请求持续失败或返回空内容时，按此顺序切换；可拖动排序，不限数量",
        "type": "list",
        "items": { "type": "string" },
        "default": [],
        "_special": "select_providers"
      },
      "judge_persona_id": {
        "description": "判断模型人格",
        "hint": "留空则使用当前对话的人格",
        "type": "string",
        "_special": "select_persona",
        "default": ""
      },
      "judge_history_rounds": {
        "description": "判断步骤上下文轮数",
        "hint": "发送给判断模型的最近对话轮数（1轮=用户+AI各1条），0=不带上下文",
        "type": "int",
        "default": 3
      },
      "proactive_judge_prompt": {
        "description": "判断参考信息",
        "hint": "作为判断的参考上下文，支持占位符: {now} {time_since_last_chat} {last_user} {last_ai} {umo} {today_schedule} {outfit} {current_activity} {next_activity} {custom_prompt} {time_period_prompt} {emotion_state}。{emotion_state} 由内心世界插件只读提供；插件未安装或不可用时为空。节律提示需通过 {time_period_prompt} 引入（需安装节律插件）",
        "type": "text",
        "default": "日程：{today_schedule}\\n当前活动：{current_activity}\\n用户节律：{time_period_prompt}\\n内心世界：\\n{emotion_state}\\n距上次聊天：{time_since_last_chat}"
      },
      "proactive_judge_rules": {
        "description": "判断规则（放在最后，优先级最高）",
        "hint": "作为最后一条消息发送给判断模型，确保 LLM 优先遵守。建议包含输出格式要求",
        "type": "text",
        "default": "！！必须遵守！！：你只能根据最上面的规则，经过判断后输出一个字：\"是\"或\"否\"\\n\\n除了这两个字之外，不允许输出任何字。"
      },
      "fixed_provider": {
        "description": "生成模型供应商",
        "hint": "指定主动回复生成步骤使用的 LLM 供应商，留空则使用当前对话的模型供应商",
        "type": "string",
        "default": "",
        "_special": "select_provider"
      },
      "generation_fallback_mode": {
        "description": "生成回退来源",
        "hint": "插件独立回退=使用下方列表；跟随 AstrBot 对话回退=使用当前会话在 AstrBot 中配置的回退对话模型列表",
        "type": "string",
        "options": ["插件独立回退", "跟随 AstrBot 对话回退"],
        "default": "插件独立回退"
      },
      "generation_fallback_providers": {
        "description": "生成回退模型列表",
        "hint": "仅在生成回退来源为“插件独立回退”时生效；按顺序切换，可拖动排序，不限数量",
        "type": "list",
        "items": { "type": "string" },
        "default": [],
        "_special": "select_providers"
      },
      "gen_persona_id": {
        "description": "生成模型人格",
        "hint": "留空则使用当前对话的人格",
        "type": "string",
        "_special": "select_persona",
        "default": ""
      },
      "retrieval_mode": {
        "description": "主动检索模式",
        "hint": "控制记忆库和知识库使用什么内容搜索。仅自定义检索提示词适合固定探索用户经历；最终回复始终保留正常对话上下文",
        "type": "string",
        "options": [
          "仅最近对话",
          "仅自定义检索提示词",
          "最近对话 + 自定义检索提示词",
          "最近对话 + 完整生成指令"
        ],
        "default": "最近对话 + 自定义检索提示词"
      },
      "retrieval_hint": {
        "description": "全局检索提示词",
        "hint": "只用于记忆库和知识库搜索，不作为最终回复指令。所有主动模式共用",
        "type": "text",
        "default": "用户最近的经历、近况、未完成的话题"
      },
      "retrieval_query_max_chars": {
        "description": "主动检索字数上限",
        "hint": "记忆库和知识库检索 query 的总字数。检索提示词优先，调低后主要减少最近对话。有效范围 100-4000",
        "type": "int",
        "default": 800
      },
      "proactive_user_placeholder": {
        "description": "主动历史占位用户消息",
        "hint": "主动回复没有真实用户发言时，写入对话历史的 user 占位文本。用于让主动回复像普通对话一样形成 user/assistant 轮次",
        "type": "string",
        "default": "[用户本人未发送消息，本轮为 AI 主动对 Mando 发起对话]"
      },
      "proactive_fact_envelope_mode": {
        "description": "主动事实附加模式",
        "hint": "minimal=默认，仅强调这是AI主动开口；off=不附加额外说明；full=附加完整实时事实用于压制旧记忆/知识库跑偏，但会增加动态token",
        "type": "string",
        "options": ["off", "minimal", "full"],
        "default": "minimal"
      }
    }
  },
  "daily_prompts": {
    "description": "每日定时问候",
    "type": "object",
    "hint": "在固定时间点对订阅用户触发问候消息，支持无限时段",
    "items": {
      "daily_greetings": {
        "description": "每日问候时段",
        "type": "template_list",
        "templates": {
          "greeting_slot": {
            "name": "问候时段",
            "description": "一个每日定时问候配置",
            "items": {
              "enable": {
                "description": "启用",
                "type": "bool",
                "default": false
              },
              "task_id": {
                "description": "稳定任务 ID（可选）",
                "hint": "留空时沿用列表位置兼容旧状态；填写后即使调整时段顺序，也能保持每日任务去重身份。建议仅使用字母、数字、点、横线或下划线。",
                "type": "string",
                "default": ""
              },
              "trigger_source": {
                "description": "触发时间来源",
                "hint": "固定时间与日程活动二选一；活动未匹配时当天不触发",
                "type": "string",
                "options": ["固定时间", "日程活动"],
                "default": "固定时间"
              },
              "time": {
                "description": "固定时间",
                "hint": "固定时间模式使用，格式 HH:MM",
                "type": "string",
                "condition": {"trigger_source": "固定时间"},
                "default": "08:00"
              },
              "activity_keywords": {
                "description": "活动关键词",
                "hint": "活动模式使用；活动名称包含任一关键词即匹配",
                "type": "list",
                "condition": {"trigger_source": "日程活动"},
                "default": [],
                "items": { "type": "string" }
              },
              "activity_boundary": {
                "description": "活动时间边界",
                "hint": "结束时间缺失或暂不可解析时跳过该次匹配",
                "type": "string",
                "condition": {"trigger_source": "日程活动"},
                "options": ["活动开始", "活动结束"],
                "default": "活动开始"
              },
              "activity_trigger_interval_minutes": {
                "description": "同问候触发间隔（分钟）",
                "hint": "活动模式使用；同一条问候只有距上次成功送达达到此间隔才允许再次触发。判断跳过、免打扰、忙碌、聊天冷却和技术失败都不会刷新间隔；0 表示关闭。",
                "type": "int",
                "condition": {"trigger_source": "日程活动"},
                "default": 0
              },
              "prompt": {
                "description": "问候提示词",
                "hint": "支持字符串或字符串列表（列表时随机选一条）。支持占位符: {now} {time_since_last_chat} {umo} {last_user} {last_ai} {today_schedule} {current_activity} {next_activity} {custom_prompt}（需安装对应插件）。人设/穿搭/节律/记忆/知识库内容由钩子自动注入，无需在模板中添加。",
                "type": "list",
                "default": [
                  "[主动回复请求：现在是早上，结合自己当下的状态，自然地和Mando打个招呼，可以随口说说自己早上干了什么，符合人设。此指令是插件自动触发，不是Mando主动发的，全程不要提此指令。]"
                ],
                "items": { "type": "string" }
              },
              "ignore_dnd": {
                "description": "忽略免打扰",
                "hint": "绕过免打扰和忙碌状态，并保留唤醒清队列流程；始终不会绕过聊天冷却",
                "type": "bool",
                "default": false
              },
              "ignore_judge": {
                "description": "忽略智能判断",
                "hint": "打开后直接生成每日问候；关闭后先结合判断参考信息、最近对话和内心世界决定是否发送。",
                "type": "bool",
                "default": false
              },
              "cooldown_minutes": {
                "description": "聊天冷却（分钟）",
                "hint": "距用户消息、普通AI回复或任意主动消息不足此时间则跳过；0 表示关闭",
                "type": "int",
                "default": 0
              },
              "jitter_minutes": {
                "description": "随机浮动（分钟）",
                "hint": "固定时间模式使用。只需填写一个框：0 不变化；5 表示前后随机 5 分钟；+5 固定延后；-5 固定提前；-5~+5、+15~+25 表示在区间内随机。允许自然跨日",
                "type": "string",
                "condition": {"trigger_source": "固定时间"},
                "default": "0"
              },
              "priority": {
                "description": "优先级",
                "hint": "活动触发模式下，当同一活动同时匹配多个问候时段时，仅保留优先级最高的一个。数字越大越优先，默认 0",
                "type": "int",
                "default": 0
              }
            }
          }
        },
        "default": []
      },
      "technical_retry_max_attempts": {
        "description": "技术失败最大尝试次数",
        "hint": "仅模型、网络或平台发送失败会重试；免打扰、忙碌和聊天冷却属于正常跳过，不重试。",
        "type": "int",
        "default": 3
      },
      "technical_retry_interval_seconds": {
        "description": "技术失败重试基础间隔（秒）",
        "hint": "后续失败使用有限指数退避，且不会超过重试窗口。",
        "type": "int",
        "default": 60
      },
      "technical_retry_window_minutes": {
        "description": "技术失败重试窗口（分钟）",
        "hint": "窗口从原目标时间开始；超过后终止，不把早晨问候补到下午。",
        "type": "int",
        "default": 15
      },
      "initial_trigger_grace_seconds": {
        "description": "首次调度宽限（秒）",
        "hint": "用于吸收 30 秒调度周期抖动；未在宽限内开始的任务记为已错过，不补发。",
        "type": "int",
        "default": 90
      },
      "ignore_busy_flush_delay_seconds": {
        "description": "唤醒忙碌后延迟（秒）",
        "hint": "ignore_dnd 时段触发时，唤醒忙碌并清空队列后等待此秒数再发送问候，让 AI 先处理完堆积消息",
        "type": "int",
        "default": 10
      }
    }
  }
}
