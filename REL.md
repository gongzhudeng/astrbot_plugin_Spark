## v2.2.1

### fix: 修复活动每日问候运行链路

**修复两个相互独立的问题**：

**故障 A：活动任务投影为空**
- 运行中的 `busy_schedule` 未提供结构化时间线接口，导致活动任务投影为空且不触发
- 加固 `busy_schedule` 时间线接口生命周期与 Spark 的加载顺序容错

**故障 B：状态命令使用旧返回结构**
- `/主动状态` 仍按旧四元组解包 `DailyGreetingTask`，投影一旦恢复为非空就会抛出 `TypeError`
- 移除旧四元组解包，统一使用 `task.target`、`task.tag` 等属性

**改进**：
- 扩展 `DailyGreetingTask` 携带配置序号、配置名称、来源日期、匹配活动、出现序号、活动边界、目标时间及投影状态
- 区分并保留"接口不可用、接口调用失败、无日程、未匹配、边界无效、已生成任务"等结果
- 展示今天全部活动投影，每项显示状态：待触发、已错过、已发送、冷却跳过、边界无效、未匹配、时间线接口不可用
